# Google Colab Requirements
# Optimized and tested for Colab environment

# Core Transformers and LLM
transformers>=4.53.0  # Required for Gemma-3n
torch>=2.0.0
sentence-transformers>=2.7.0
accelerate>=0.25.0

# Vector Database (IMPORTANT: Must be >=1.7.0 for QueryVector)
qdrant-client>=1.10.0
fastembed>=0.3.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
tiktoken>=0.5.0
aiohttp>=3.9.0

# Note: vLLM and LangChain/LlamaIndex are optional for Colab
# They are NOT required for the Gemma-based Colab setup
